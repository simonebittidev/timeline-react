export enum DotColorsEnum  {
    Dark = "timeline-point bg-dark",
    Warning = "timeline-point bg-warning",
    Error = "timeline-point bg-danger",
    Success = "timeline-point bg-success",
    Blue = "timeline-point bg-primary",
}

export enum TimeLineItemPosition {
    Left = "timeline-item timeline-item-left",
    Rigth = "timeline-item timeline-item-right",
}

export enum TimeLineItemTimePositionEnum  {
    Left = "time-left",
    Rigth = "time-right",
}

export enum TextColor  {
    Dark = "text-dark",
    Warning = "text-warning",
    Error = "text-danger",
    Success = "text-success"
}

export enum TextPosition{
    Start = "text-start",
    End = "text-end"
}
